# OnlyTalk 聊天支持类型与消息格式文档

> 整理自 `only-talk-app` 代码仓库（Tauri + React/Vue + Rust 后端）。
> 核心代码位置：`packages/types`（前端类型）、`src-tauri/src`（Rust 后端）、`apps/pc` 与 `apps/mobile`（前端）。

---

## 目录

1. [总体架构与数据流](#1-总体架构与数据流)
2. [聊天支持类型总表（text_type）](#2-聊天支持类型总表text_type)
3. [消息内容格式（raw 字段）](#3-消息内容格式raw-字段)
   - 3.1 单聊文本 / 图片 / 文件
   - 3.2 群聊文本 / 图片 / 文件
   - 3.3 WebRTC 信令
   - 3.4 P2P（隐私模式）消息
4. [本地存储格式](#4-本地存储格式)
   - 4.1 数据库（SQLite/SQLCipher）
   - 4.2 消息表结构
   - 4.3 文件落盘
5. [后端转发格式（QUIC / HTTP）](#5-后端转发格式quic--http)
   - 5.1 QUIC 消息帧
   - 5.2 消息类型与路由
   - 5.3 ACK 回执与消息链（防丢）
   - 5.4 P2P 四通道与媒体帧
   - 5.5 文件上传 / 下载（HTTP）
6. [前端渲染格式](#6-前端渲染格式)
   - 6.1 前端事件（Tauri emit）
   - 6.2 会话列表渲染
   - 6.3 聊天详情渲染
   - 6.4 发送消息格式
7. [端到端示例](#7-端到端示例)

---

## 1. 总体架构与数据流

```
┌────────────────────────────┐        ┌────────────────────────────┐
│  前端 (apps/pc 或 apps/mobile)     │        │  前端 (apps/pc 或 apps/mobile)     │
│  ────────────────           │        │  ────────────────           │
│  Tauri invoke 命令           │        │  Tauri 事件 (emit)          │
└──────────┬─────────────────┘        └──────────▲─────────────────┘
           │                                    │
           ▼                                    │
┌────────────────────────────┐        ┌────────────────────────────┐
│ Rust 后端 (src-tauri)       │        │ Rust 后端 (src-tauri)       │
│  - 本地 SQLite (SQLCipher)  │        │  - 写入 chat_record 表      │
│  - chat_record_send 发送队列 │◄──────►│  - 解析 text_type 分派      │
└──────────┬─────────────────┘        └──────────▲─────────────────┘
           │ QUIC 长连接 (UDP)                    │ QUIC 长连接 (UDP)
           ▼                                    │
┌────────────────────────────┐        ┌────────────────────────────┐
│     中心服务器 (QUIC)        │        │     中心服务器 (QUIC)        │
│  - 消息转发 / ACK 回执        │────────►│  - 好友单聊 / 群聊 / 信令转发 │
│  - P2P 地址协商              │        └────────────────────────────┘
└────────────────────────────┘
           │ (可选) P2P 直连 QUIC：Default / MediaInfo / MediaData / File 四通道
           ▼
┌────────────────────────────────────────────┐
│  对端客户端（隐私模式视频通话 / 文件直传）        │
└────────────────────────────────────────────┘
```

**消息路径**：

- **单聊/群聊（文本、图片、文件、WebRTC 信令）**：前端 → Rust `send_*_msg` 命令 → QUIC 长连接 → 中心服务器 → 转发给接收端 → 接收端 Rust 处理并落库 → `emit` 事件给前端渲染。
- **P2P（隐私模式）**：先通过服务器 QUIC 完成握手（`MSG_TYPE_P2P`/`MSG_TYPE_P2P_USER_SERVER`/`MSG_TYPE_P2P_USER_CLIENT`），协商出双方地址后建立**点对点 QUIC 连接**，走独立通道（信令/媒体/文件）。
- **WebRTC 信令**：通过中心服务器 QUIC 转发（`text_type=100`），**不走 P2P 通道**。

---

## 2. 聊天支持类型总表（text_type）

> 定义位置：`src-tauri/src/utils/message_types.rs`（Rust）与 `packages/types/src/backend/chat/index.ts`（前端）。

| text_type | 常量 | 含义 | 传输通道 | 是否落库 | 前端渲染 |
|---|---|---|---|---|---|
| 0 | — | 原生文本（历史格式，raw 即纯文本） | 单聊 QUIC | ✅ | TextBox（原文） |
| 1 | `MSG_TYPE_TEXT` | 文本消息（JSON raw） | 单聊 QUIC | ✅ | TextBox |
| 2 | `MSG_TYPE_IMAGE` | 图片消息 | 单聊 QUIC | ✅ | ChatImage |
| 3 | `MSG_TYPE_FILE` | 文件消息 | 单聊 QUIC | ✅ | ChatFile |
| 4 | `MSG_TYPE_P2P` | P2P 连接初始化（隐私模式握手） | 单聊 QUIC | ❌（不落库） | 会话列表显示 `[隐私模式]` |
| 5 | `MSG_TYPE_P2P_VIDEO_CALL` | P2P 视频通话（旧） | P2P 直连 | 视情况 | WebRTCMessage |
| 6 | `MSG_TYPE_P2P_VIDEO_DATA` | P2P 视频帧数据 | P2P MediaData | ❌ | — |
| 7 | `MSG_TYPE_P2P_VIDEO_CONFIG` | P2P 视频参数配置 | P2P 直连 | ❌ | — |
| 8 | `MSG_TYPE_P2P_TEXT` | 隐私聊天文本 | P2P Default | ✅ | PrivacyModeMessage |
| 9 | `MSG_TYPE_P2P_AUDIO_DATA` | P2P 音频帧数据（Opus） | P2P MediaData | ❌ | — |
| 10 | `MSG_TYPE_P2P_MEDIA_CONFIG` | P2P 媒体配置协商 | P2P 直连 | ❌ | — |
| 11 | `MSG_TYPE_P2P_MEDIA_CONTROL` | P2P 媒体控制（开关/暂停/结束） | P2P MediaInfo | ❌ | — |
| 12 | `MSG_TYPE_P2P_VIDEO_CALL_INVITE` | 视频通话邀请 | 单聊 QUIC | ✅ | WebRTCMessage |
| 13 | `MSG_TYPE_P2P_VIDEO_CALL_ACCEPT` | 接受通话邀请 | 单聊 QUIC | ✅ | WebRTCMessage |
| 14 | `MSG_TYPE_P2P_VIDEO_CALL_REJECT` | 拒绝通话邀请 | 单聊 QUIC | ✅ | WebRTCMessage |
| 15 | `MSG_TYPE_P2P_VIDEO_CALL_END` | 结束通话 | 单聊 QUIC | ✅ | WebRTCMessage |
| 16 | `MSG_TYPE_P2P_MEDIA_INFO` | 媒体状态信息（分辨率/码率等） | P2P MediaInfo | ❌ | — |
| 17 | `MSG_TYPE_P2P_FILE_DATA` | P2P 文件分片数据 | P2P File | ❌ | — |
| 18 | `MSG_TYPE_P2P_FILE_TRANSFER_REQUEST` | P2P 文件传输握手请求 | P2P File | ❌ | — |
| 19 | `MSG_TYPE_P2P_FILE_TRANSFER_RESPONSE` | P2P 文件传输握手响应 | P2P File | ❌ | — |
| 20 | `MSG_TYPE_P2P_MEDIA_READY` | 媒体接收就绪 | P2P 直连 | ❌ | — |
| 88 | `MSG_TYPE_JSON` | JSON 消息（预留） | 单聊 QUIC | ❌ | — |
| 99 | `MSG_TYPE_PING` | 心跳（ping） | 单聊 QUIC | ❌ | — |
| 100 | `MSG_TYPE_WEBRTC_SIGNAL` | **WebRTC 信令**（offer/answer/candidate） | 单聊 QUIC（服务器转发） | ✅（WebRTCSignalRecord） | WebRTCMessage |
| 103 | `P2P_ACCEPT_REQUEST` | P2P 接受请求 | 单聊 QUIC | ❌ | — |
| 104 | `P2P_REJECT_REQUEST` | P2P 拒绝请求 | 单聊 QUIC | ❌ | — |
| 201 | `MSG_TYPE_RECALL_SUCCESS` | 消息接收成功回执（单聊） | 单聊 QUIC | ❌（触发落库） | — |
| 202 | `MSG_TYPE_RECALL_FAILURE` | 消息接收失败回执 | 单聊 QUIC | ❌ | — |
| 203 | `MSG_TYPE_P2P_USER_SERVER` | 本机作为 P2P 服务端建立连接 | 单聊 QUIC | ❌ | — |
| 204 | `MSG_TYPE_P2P_USER_CLIENT` | 本机作为 P2P 客户端建立连接 | 单聊 QUIC | ❌ | — |
| 1024 | `NOTIFY_TYPE_MSG` | 通知消息（好友申请等） | 单聊 QUIC | ✅（system_notification） | NotificationPanel |
| 2001 | `MSG_TYPE_GROUP_TEXT` | **群文本消息** | 群聊 QUIC | ✅（group_chat_record） | TextBox |
| 2002 | `MSG_TYPE_GROUP_IMAGE` | **群图片消息** | 群聊 QUIC | ✅ | ChatImage |
| 2003 | `MSG_TYPE_GROUP_FILE` | **群文件消息** | 群聊 QUIC | ✅ | ChatFile |
| 2004 | `MSG_TYPE_GROUP_NOTIFICATION` | 群通知（成员变更等） | 群聊 QUIC | 视情况 | 会话列表显示 `[群通知]` |
| 2005 | `MSG_TYPE_GROUP_READ` | 群消息已读回执 | 群聊 QUIC | ❌ | — |
| 2201 | `GROUP_MSG_TYPE_RECALL_SUCCESS` | 群消息接收成功回执 | 群聊 QUIC | ❌（触发落库） | — |
| 10001 | `MSG_TYPE_SYSTEM` | 系统消息 | 单聊 QUIC | ❌ | system_message 事件 |

**要点**：

- **支持的能力**：文本 ✅、图片 ✅、文件 ✅、WebRTC 信令 ✅（通过服务器 QUIC 转发）、P2P 隐私聊天（文本/音视频/文件直传）✅、群聊（文本/图片/文件/通知/已读）✅、系统通知 ✅。
- 单聊与群聊使用**不同的 text_type 值域**（单聊 1/2/3，群聊 2001/2002/2003），便于后端路由和前端渲染区分。
- 回执类（201/2201）不落库，收到后触发把"发送队列"中的消息正式写入聊天记录表。

---

## 3. 消息内容格式（raw 字段）

`raw` 是 `TextQuicMsgVo.raw`（String，JSON 序列化），**单聊**与**群聊**的结构不同：

### 3.1 单聊（text_type = 1 / 2 / 3）

> 定义位置：`src-tauri/src/entity/chat_record_raw.rs`（Rust）、`packages/types/src/backend/chat/index.ts`（前端）。

**文本（1）**：

```json
{
  "prev_id": "上一条已确认消息的 nano_id，链头为全 0 UUID",
  "text": "消息内容",
  "platform": 0
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `prev_id` | string | 上一条消息 id（**消息链**，见 §5.3）；链头为 `00000000-0000-0000-0000-000000000000` |
| `text` | string | 消息文本 |
| `platform` | number | 0=PC，1=Mobile |

**图片（2）**：

```json
{
  "prev_id": "…",
  "biz_id": "文件服务器业务ID（UUID）",
  "file_name": "原文件名",
  "is_preview": false,
  "img_width": 1280,
  "img_height": 720,
  "img_size": 102400,
  "platform": 0
}
```

**文件（3）**：

```json
{
  "prev_id": "…",
  "biz_id": "文件服务器业务ID（UUID）",
  "file_name": "原文件名",
  "file_size": 1048576,
  "file_type": "pdf",
  "platform": 0
}
```

> `biz_id` 用于向文件服务器换取下载链接；接收端按 `biz_id` 下载文件到本地并建立 `file_record` 映射。

### 3.2 群聊（text_type = 2001 / 2002 / 2003）

> 与单聊不同：群聊 raw **不含 prev_id、不含 platform**，改为带 `send_user`（因为群消息需要知道实际发送人）。

**群文本（2001）**：

```json
{ "text": "消息内容", "send_user": "发送者 UUID" }
```

**群图片（2002）**：

```json
{
  "biz_id": "…",
  "file_name": "image.png",
  "img_width": 1280,
  "img_height": 720,
  "img_size": 102400,
  "send_user": "发送者 UUID"
}
```

**群文件（2003）**：

```json
{
  "biz_id": "…",
  "file_name": "report.pdf",
  "file_size": 1048576,
  "file_type": "pdf",
  "send_user": "发送者 UUID"
}
```

### 3.3 WebRTC 信令（text_type = 100）

> 定义位置：`packages/types/src/webrtc/index.ts`（`WebRTCSignalMessage`）、`src-tauri/src/entity/chat_record_raw.rs`（`WebRTCSignalRecord`）。

```json
{
  "prev_id": "…（发送后由后端补充）",
  "type": "offer | answer | candidate",
  "sender": "发送方用户 UUID",
  "receiver": "接收方用户 UUID",
  "sessionId": "一次 P2P 连接的会话 ID",
  "data": {
    "type": "offer|answer",
    "sdp": "…"
  },
  "timestamp": 1724044800000
}
```

- `type=offer/answer` 时 `data` 为 `RTCSessionDescriptionInit`（含 `type`、`sdp`）；
- `type=candidate` 时 `data` 为 `RTCIceCandidateInit`（含 `candidate`、`sdpMid`、`sdpMLineIndex`、`usernameFragment`）。
- 后端收到后**原样转发**给前端（`webrtc_signal` 事件），同时包装为 `WebRTCSignalRecord`（Rust 侧字段为 `signal_type`/`session_id`，通过 `#[serde(rename)]` 与前端 JSON 对应）**落库**为一条聊天记录。

### 3.4 P2P（隐私模式）消息

> 定义位置：`src-tauri/src/entity/p2p_models.rs`、`packages/types/src/p2p/index.ts`。P2P 消息的 `raw` 是相应结构的 JSON，按 `text_type` 分派。

**握手（text_type=4，P2pInitMsg）**：

```json
{
  "accept_addr": "接收方地址 IP:Port",
  "request_addr": "请求方地址 IP:Port",
  "request_uuid": "请求方 UUID",
  "request_token": "请求方 token（鉴权）",
  "accept_uuid": "接收方 UUID",
  "accept": true,
  "ip_type": 1,
  "step": 3,
  "is_server": false
}
```

**文件传输（text_type=18/19/17）**：

```json
// 请求（18）
{
  "transfer_id": "传输ID",
  "file_name": "a.zip",
  "mime_type": "application/zip",
  "total_size": 10485760,
  "total_chunks": 20,
  "timestamp": 1724044800000
}
// 响应（19）
{ "transfer_id": "…", "accept": true, "timestamp": 1724044800000 }
// 分片（17）
{
  "uuid": "目标用户 UUID",
  "file_name": "a.zip",
  "mime_type": "application/zip",
  "total_size": 10485760,
  "chunk_index": 0,
  "total_chunks": 20,
  "chunk_data": "<二进制分片，前端类型为 Base64>",
  "transfer_id": "…"
}
```

**媒体控制 / 信息（11 / 16）**：

```json
// 媒体控制（11）
{ "control_type": "VideoToggle | AudioToggle | Pause | Resume | EndCall", "enabled": true, "timestamp": 1724044800000 }
// 媒体信息（16）
{ "info_type": "ResolutionChange | BitrateChange | FrameRateStats | NetworkQuality | EncoderInfo", "data": "JSON字符串", "timestamp": 1724044800000 }
```

**视频通话邀请/响应（12/13/14/15）**：

```json
// 邀请（12）
{
  "from_uuid": "邀请者 UUID",
  "to_uuid": "被邀请者 UUID",
  "timestamp": 1724044800000,
  "media_config": { "video_config": {…}, "audio_config": {…}, "buffer_config": {…} },
  "from_name": "昵称"
}
// 响应（13 接受 / 14 拒绝）
{
  "from_uuid": "响应者 UUID",
  "to_uuid": "邀请者 UUID",
  "accept": true,
  "timestamp": 1724044800000,
  "media_config": {…},
  "reject_reason": "可选"
}
```

---

## 4. 本地存储格式

### 4.1 数据库（SQLite + SQLCipher）

> 定义位置：`src-tauri/src/dao/init_private_db.rs`、`utils/global_static_str.rs`。

| 数据库 | 路径 | 加密 | 内容 |
|---|---|---|---|
| `common.db` | `<app_path>/dbData/<用户UUID>/common.db` | 否 | `file_record`（文件元数据） |
| `user.db` | `<app_path>/dbData/<用户UUID>/user.db` | 否 | `user_info`、`user_token` |
| `private.db` | `<app_path>/dbData/<用户UUID>/private.db` | **SQLCipher 加密**（密钥 `PRIVATE_DB_KEY = "myJB18cm"`） | 聊天记录、会话、好友、群组等全部业务数据 |

- `app_path` 由 Tauri 应用数据目录提供（`get_config(APP_PATH)`）。
- 每个用户一个目录，多用户登录互相隔离；`platform`（PC=0/Mobile=1）同一平台互斥登录。
- 文件资源目录：`<app_path>/resource/monthly_resources/<uuid>.<ext>`（按月分目录，如 `monthly_resources/`）。

### 4.2 消息表结构

> 定义位置：`src-tauri/src/entity/*.rs`（`impl SqliteStore::create_table`）。

**`chat_record`（单聊聊天记录，加密库）**

| 列 | 类型 | 说明 |
|---|---|---|
| `id` | INTEGER PK AUTOINCREMENT | 自增主键 |
| `nano_id` | TEXT NOT NULL UNIQUE | 消息唯一 id |
| `raw` | TEXT NOT NULL | 消息内容 JSON（§3） |
| `timestamp` | INTEGER NOT NULL | 毫秒时间戳 |
| `send_user` | TEXT NOT NULL | 发送者 UUID |
| `recv_user` | TEXT NOT NULL | 接收者 UUID |
| `text_type` | INTEGER NOT NULL DEFAULT 0 | 消息类型（§2） |

**`group_chat_record`（群聊聊天记录）**：同上，`recv_user` 换为 `group_id`（列名 `group_id`），无 `recv_user`。

**`chat_session`（会话列表）**

| 列 | 类型 | 说明 |
|---|---|---|
| `id` / `nano_id` | INTEGER / TEXT | — |
| `timestamp` / `text_type` | INTEGER | 最后一条消息的时间/类型 |
| `unread_count` | INTEGER | 未读数 |
| `last_message` | TEXT | 最后一条消息的 raw（用于会话列表预览） |
| `send_user` / `recv_user` | TEXT | `UNIQUE(send_user, recv_user)` |
| `session_type` | INTEGER | 1-单聊，2-群聊，3-系统，4-公众号 |
| `is_show` / `is_top` | INTEGER | 显示/置顶 |
| `group_id` | TEXT NULL | 群聊会话时为群 UUID |

**`chat_record_send`（发送队列，防丢机制核心）**

| 列 | 类型 | 说明 |
|---|---|---|
| `send_id` | TEXT | 发送时的 nano_id（本地生成） |
| `msg_id` | TEXT | 服务器确认后的消息 id（= nano_id） |
| `text_type` / `platform` | INTEGER | 消息类型 / 发送平台 |
| `recv_user` / `send_user` | TEXT | 收发双方 |
| `raw` | TEXT | 消息内容（**已带 prev_id**） |
| `timestamp` | INTEGER | 超 1 分钟未确认标记失败 |
| `send_status` | INTEGER | 0-未发送，1-发送中，2-发送失败，3-发送成功 |
| `retry_count` | INTEGER | 重试次数，>3 标记失败 |

配套表：`chat_record_ack`（ack 表：`send_id` ↔ `msg_id`、`prev_id`、`ack_status`）、`chat_record_read`（已读）、`group_message_ack` / `group_message_read`（群版）、`system_notification`（通知）、`friend` / `group` / `group_member`（关系数据）。

**`file_record`（文件元数据，公共库）**

| 列 | 类型 | 说明 |
|---|---|---|
| `id` / `biz_id` | INTEGER / TEXT | 主键 / 文件业务 ID |
| `uuid` | TEXT | 本地文件 uuid（落盘文件名） |
| `file_name` / `file_path` | TEXT | 文件名 / 本地绝对路径 |
| `file_size` / `mime_type` | INTEGER / TEXT | 大小 / MIME |
| `file_hash` | TEXT | SHA256（去重） |
| `status` | INTEGER | 0-正常，1-已删除，2-临时文件，3-下载失败超重试上限 |
| `download_retry_count` | INTEGER | 下载失败重试次数（迁移列） |
| `created_at` / `updated_at` | INTEGER | 时间 |

### 4.3 文件落盘

- **接收方**：按 `biz_id` 请求下载链接 → 带 token GET 二进制 → 计算 SHA256 → 以 `uuid_v4.ext` 命名存入 `monthly_resources/` → 插入 `file_record`。
- **发送方**：图片先压缩为 WebP（`compress_image_to_webp`）再上传；文件原样上传。
- 前端通过 `FileVo.tauri_file_path` 拿到本地路径用于渲染（`getChatFileByBizId` 命令，命中 `file_record` 后返回；未命中则触发下载）。

---

## 5. 后端转发格式（QUIC / HTTP）

### 5.1 QUIC 消息帧

> 定义位置：`src-tauri/src/entity/text_msg.rs`、`quic_service/center_service/text_msg_service.rs`。

单聊/群聊消息通过 QUIC 长连接（quinn 库，DNS 动态解析 `onlytalk.cn`，UDP 端口 9562/9563/9564/9565）发送，帧结构：

```
┌──────────────────────────── HeadMsg (bincode, 9 字节) ────────────────────────────┐
│ version: u8  │ crc: u16 (X25)  │ body_len: u32  │ message_type: u16  │
└──────────────────────────────────────────────────────────────────────────────────┘
┌────────────────────────── TextQuicMsg (bincode 序列化) ──────────────────────────┐
│ nano_id: String │ text_type: u16 │ raw: Vec<u8> │ recv_user: String │
│ send_user: String │ timestamp: i64 │
└──────────────────────────────────────────────────────────────────────────────────┘
```

- **`message_type`**（帧头）与 **`text_type`**（消息体）取值一致（`MSG_TYPE_TEXT=1` 等）。
- **CRC**：对消息体做 X25 校验和，防篡改/防粘包错位；接收端校验失败即丢弃。
- **粘包处理**：单条 QUIC 流上可连续写多条消息；接收端按 `body_len` 切帧，残包缓存进 `buffer_msg` 等待下个数据包。
- 发送走**单向流**（`open_uni()` + `write_all` + `finish`）。

### 5.2 消息类型与路由

接收端 `process_msg`（`process_text_msg_from_server.rs`）按 `text_type` 分派：

| text_type | 处理 |
|---|---|
| 1 / 2 / 3 | 单聊：`process_private_chat_message` → 落库 `chat_record` → `emit("text_message")` → 更新/创建会话 |
| 2001 / 2002 / 2003 | 群聊：`process_group_chat_message` → 落库 `group_chat_record` → emit → 更新会话 |
| 4 | P2P 握手：解析 `P2pInitMsg` → `process_p2p_msg` |
| 99 | Ping：记录心跳，重置断连计数 |
| 100 | WebRTC 信令：解析 candidate → emit `webrtc_signal` → 落库（WebRTCSignalRecord） |
| 201 / 2201 | 单聊/群聊 ACK：把发送队列中的消息正式写入聊天记录，标记发送成功，触发补发队列 |
| 203 / 204 | 以服务端/客户端身份建立 P2P QUIC 连接 |
| 1024 | 通知：落库 `system_notification` → emit → 按 level1/level2 分派（好友申请/群相关） |
| 10001 | 系统消息：emit `system_message` |

### 5.3 ACK 回执与消息链（防丢）

> 参考仓库根目录 `聊天记录链存储防丢.md`。

**发送流程**（`send_text_msg_service`）：

1. 前端构造 `TextQuicMsgVo`（`raw` 中 prev_id 置空），`invoke('send_text_msg')`。
2. 后端查询与接收方最近一条**已确认**消息，取其 `msg_id` 作为本次 `prev_id`（无则全 0 UUID）。
3. 更新 raw 中的 `prev_id`（`set_prev_id`，支持类型 1/2/3/100）→ 写入 `chat_record_send`（status=1）+ `chat_record_ack`（ack_status=0）→ 组帧发 QUIC。
4. 收到 `MSG_TYPE_RECALL_SUCCESS(201)` 回执：将发送队列消息（按 send_id 找回原 raw）插入 `chat_record` → 更新 ack/send 状态为成功 → 触发 `process_no_send_success_msg` 补发剩余排队消息。
5. 失败重试：status=1 且 8 秒未确认 → 重发（最多 3 次）；>1 分钟未确认标记失败；同接收方存在未完成消息时后续消息进入等待队列（status=0）。

**消息链**：单聊每条消息通过 `prev_id` 串成链；`00000000-0000-0000-0000-000000000000` 为链头；与上一条成功消息间隔超 **48 小时**开新链。链断开时向服务器请求同步历史消息。

### 5.4 P2P 四通道与媒体帧

> 定义位置：`src-tauri/src/quic_service/p2p_service/p2p_quic_service.rs`、`entity/p2p_models.rs`。

P2P 连接建立后，每个通道类型对应 QUIC 上的一个独立双向流：

| 通道 | 用途 |
|---|---|
| `Default` | 信令、文本消息、控制命令 |
| `MediaInfo` | 媒体状态信息（分辨率/码率/帧率/网络质量） |
| `MediaData` | 视频帧、音频帧 |
| `File` | 文件分片数据与传输握手 |

**MediaData 轻量帧头**（替代通用 HeadMsg+TextQuicMsg）：

```
┌─────────────── MediaFrameHeader (固定 5 字节) ───────────────┐
│ frame_type: u8 (1=视频, 2=音频) │ data_len: u32 (大端序) │ 帧体原始数据… │
└──────────────────────────────────────────────────────────────┘
```

### 5.5 文件上传 / 下载（HTTP）

> 定义位置：`src-tauri/src/service/chat_service.rs`（上传）、`service/file_service.rs` + `cmd/file_controller.rs`（下载）。

**上传**（发送图片/文件时，multipart POST）：

```
POST {TALK_API}/file_integrated/upload/user_chat/{friend_uuid}     // 单聊
POST {TALK_API}/file_integrated/upload/group_chat/{group_uuid}     // 群聊
```

响应：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "uuid": "业务UUID",
    "biz_name": "…", "biz_type": "user_chat|group_chat",
    "description": "…", "remark": "…",
    "file_infos": [
      { "biz_id": "消息raw中使用的biz_id", "origin_file_id": "…", "file_id": "…" }
    ]
  }
}
```

**下载**（接收/预览时）：

```
POST {TALK_API}/file/download_link/chat_biz/{biz_id}/1     // 单聊/群聊文件
POST {TALK_API}/file/download_link/pub_biz/{biz_id}        // 公共文件（头像等）
```

→ 返回 URL 列表 → 按桶类型带 token 或不带 token GET 二进制（公开桶 `/user-avatar/`、`/group-avatar/` 免 token）→ 落盘 + 写 `file_record`。

---

## 6. 前端渲染格式

### 6.1 前端事件（Tauri emit → listen）

| 事件名 | 载荷 | 触发场景 |
|---|---|---|
| `text_message` | `TextQuicMsgVo` JSON | 收到/确认单聊、群聊消息 |
| `chat_session` | `ChatSessionEvent {type: 0|1, data: ChatSessionVo}` | 会话更新/创建 |
| `webrtc_signal` | `TextQuicMsgVo` JSON（raw 为信令） | 收到 WebRTC 信令 |
| `group_message_ack` | `TextQuicMsgVo` | 群消息送达 |
| `system_message` | `TextQuicMsgVo` | 系统消息 |
| 通知相关 | `SystemNotification` | 通知面板 |

前端统一类型（`packages/types`）：

```ts
interface TextQuicMsgVo {
  nano_id: string;   // 消息 id
  text_type: number; // 1 文本, 2 图片, 3 文件, 100 WebRTC 信令, 2001-2003 群聊等
  raw: string;       // JSON 字符串（§3）
  recv_user: string;
  send_user: string;
  timestamp: number;
}
```

### 6.2 会话列表渲染（MessageBox）

`apps/pc/src/pages/Home/Chats/components/MessageBox.tsx` 按 `text_type` 生成预览文本：

| text_type | 预览 |
|---|---|
| 2 / 2002 | `[图片]` |
| 3 / 2003 | `[文件]` |
| 4 | `[隐私模式]` |
| 5 | `[视频通话]` |
| 12 | `[视频通话邀请]` |
| 13 | `[已接听]` |
| 14 | `[已拒绝]` |
| 15 | `[通话结束]` |
| 100 | `[WebRTC信令]` |
| 2004 | `[群通知]` |
| 其他 | `JSON.parse(raw).text`，失败则显示原文 |

### 6.3 聊天详情渲染

**PC 端**（`apps/pc`，React + Ant Design）：

| text_type | 组件 | 说明 |
|---|---|---|
| 1 | `TextBox` | `JSON.parse(raw)` 取 `text`（兼容 `content`），失败显示原文；保留换行/缩进 |
| 2 | `ChatImage` | 用 `biz_id` 调 `getChatFileByBizId(bizId, nano_id)` 拿到 `tauri_file_path` 渲染；图片缓存于 Map |
| 3 | `ChatFile` | 文件卡片（图标颜色按扩展名分类、大小格式化）；操作：打开文件 / 在文件夹中显示 |
| 4 | `PrivacyModeMessage` | 隐私模式入口 |
| 5, 12, 13, 14, 15, 100 | `WebRTCMessage` | 通话/信令状态展示 |

**移动端**（`apps/mobile`，Vue）：同一套 Tauri 命令与 `raw` 格式；文本 `JSON.stringify({text, prev_id: "", platform: 0})`；图片先压缩/复制（`content://` URI 复制到临时目录）再 `send_image_msg`。

### 6.4 发送消息格式

前端统一构造 `TextQuicMsgVo` 后 `invoke` 对应 Tauri 命令：

| 命令 | text_type | raw（发送时） | 后端处理 |
|---|---|---|---|
| `send_text_msg` | 1 | `{"text":"…","prev_id":"","platform":0}` | 补 prev_id/send_user → QUIC |
| `send_image_msg` | 2 | **本地文件路径** | WebP 压缩 → 上传 → 组装 ImageRecord → QUIC |
| `send_file_msg` | 3 | **本地文件路径** | 上传 → 组装 FileRecord → QUIC |
| `send_group_text_msg` | 2001 | `{"text":"…"}` | 组装 GroupTextRecord → QUIC |
| `send_group_image_msg` | 2002 | 本地文件路径 | 压缩→上传→GroupImageRecord→QUIC |
| `send_group_file_msg` | 2003 | 本地文件路径 | 上传→GroupFileRecord→QUIC |
| `send_text_msg`（WebRTC） | 100 | 信令 JSON（§3.3） | 补 prev_id → QUIC 服务器转发 |
| `send_init_p2p_udp` / `send_p2p_init_msg` | 4 | — | P2P 握手 |

> 注意：WebRTC 信令复用 `send_text_msg`（`webrtcService/index.ts` `sendSignal`），`raw = JSON.stringify(WebRTCSignalMessage)`。

---

## 7. 端到端示例

**文本消息 A → B（经服务器）**：

```
A 前端: invoke('send_text_msg', {nano_id: nanoid(), text_type: 1,
        raw: '{"text":"你好","prev_id":"","platform":0}', recv_user: B, send_user: '', timestamp})
   ↓
A Rust: 查最近已确认消息 → prev_id 写入 raw → chat_record_send(0/1状态) + chat_record_ack
   ↓ HeadMsg(bincode, crc=X25) + TextQuicMsg(bincode)
   ↓ QUIC 单向流 → 中心服务器 → B 的 QUIC 长连接
B Rust: 校验 crc → 解析 → 落库 chat_record → emit("text_message")
   ↓
B 前端: 监听 text_message → MessageList → CustomerChatBox → TextBox 渲染
   ↓ 同时 B 回 201 ACK
A Rust: 收到 201 → 将发送队列消息正式插入 chat_record → 更新状态 → 前端会话显示
```

**WebRTC 信令（offer）A → B**：

```
A 前端: WebRTCService.createOffer → sendSignal({type:'offer', sender, receiver, sessionId, data:sdp})
   ↓ invoke('send_text_msg', {text_type: 100, raw: JSON.stringify(signal)})
A Rust: set_prev_id(100) → QUIC 服务器转发
B Rust: process_webrtc_signal → emit("webrtc_signal") → 落库 WebRTCSignalRecord
B 前端: 监听 webrtc_signal → WebRTCService.handleOffer → 回 answer（同样路径）
   ↓ ICE candidate 双方互发（type=100, data={candidate,sdpMid,sdpMLineIndex}）
   ↓ RTCPeerConnection 建立 → RTCDataChannel 直连聊天（不再经服务器）
```

**隐私模式视频通话（P2P QUIC 直连）**：

```
A 前端: send_p2p_init_msg → 服务器 QUIC 转发 MSG_TYPE_P2P(4) P2pInitMsg 握手
双方: 协商地址/token → 203/204 建立点对点 QUIC 连接
   ↓ Default 通道: 媒体配置(10)、控制(11)
   ↓ MediaData 通道: [frame_type][data_len][帧数据] 视频帧/Opus音频帧
   ↓ File 通道: FileTransferRequest(18) → Response(19) → FileData 分片(17)
```

---

## 附：相关代码索引

| 内容 | 位置 |
|---|---|
| 消息类型常量 | `src-tauri/src/utils/message_types.rs` |
| QUIC 帧结构（HeadMsg/TextQuicMsg） | `src-tauri/src/entity/text_msg.rs` |
| 消息内容格式（Text/Image/File/WebRTC Record） | `src-tauri/src/entity/chat_record_raw.rs` |
| 消息表结构（chat_record 等） | `src-tauri/src/entity/*.rs` |
| 消息处理分派（收） | `src-tauri/src/quic_service/center_service/process_text_msg_from_server.rs` |
| 消息发送（发 + 防丢链） | `src-tauri/src/service/chat_service.rs` |
| P2P 模型（握手/媒体/文件） | `src-tauri/src/entity/p2p_models.rs` |
| P2P 通道处理 | `src-tauri/src/quic_service/p2p_service/p2p_quic_service.rs` |
| 文件下载 | `src-tauri/src/service/file_service.rs`、`cmd/file_controller.rs` |
| 前端类型定义 | `packages/types/src/backend/chat`、`webrtc`、`p2p`、`file` |
| PC 端消息渲染 | `apps/pc/src/pages/Home/Chats/components/`（MessageBox / CustomerChatBox / ChatImage / ChatFile / WebRTCMessage） |
| WebRTC 服务（前端） | `apps/pc/src/services/webrtcService/index.ts` |
| 移动端消息 | `apps/mobile/src/pages/Chats/Chat/index.vue` |
| 消息链防丢设计 | 仓库根目录 `聊天记录链存储防丢.md` |
